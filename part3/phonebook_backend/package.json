{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "backend server for phonebook react app",
  "license": "MIT",
  "author": "Isaac Pemberton",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../phonebook && npm run build && cp -r dist ../phonebook_backend",
    "deploy:full": "npm run build:ui && cd ../phonebook_backend && npm start"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.1",
    "morgan": "^1.11.0"
  }
}
